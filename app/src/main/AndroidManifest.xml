<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.APC.Reserv">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Firsttime" />
        <activity android:name=".Login" />
        <activity android:name=".Forget" />
        <activity android:name=".Register" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Feed"
            android:label="@string/title_activity_feed"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Settings"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".Cuenta"
            android:label="@string/title_activity_cuenta"
            android:theme="@style/AppTheme" />
        <activity android:name=".Cancha" />
        <activity android:name=".Fotos" />
        <activity
            android:name=".CanchaReservada"
            android:label="@string/title_activity_cancha_reservada"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AReservar"
            android:label="@string/title_activity_areservar"
            android:theme="@style/Theme.custom" />
    </application>

</manifest>